# Core dependencies
fastapi>=0.115.0
uvicorn>=0.34.0
pydantic>=2.10.0
python-dotenv>=1.0.0
requests>=2.32.0

# Trading engine dependencies
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# OAuth 2.0 and security dependencies
aiohttp>=3.8.0
PyJWT>=2.8.0
cryptography>=41.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6

# Testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Deriv API dependencies
websocket-client>=1.6.0
aiofiles>=23.2.0
ujson>=5.8.0
python-deriv-api==0.1.6

# Machine Learning dependencies (Phase 3)
xgboost>=2.1.0
scikit-learn>=1.5.0
# pandas-ta removed (requires Python 3.12+, using manual calculations instead)
numba>=0.60.0
joblib>=1.4.0

# PyTorch for CRASH500 Survival Analysis (LSTM)
# Using CPU-only version for lighter deployment
torch>=2.0.0
# If GPU available, replace with: torch>=2.0.0+cu118 --extra-index-url https://download.pytorch.org/whl/cu118

# System monitoring dependencies (Phase 5)
psutil>=6.1.0

# Metrics and monitoring dependencies (Phase 6)
prometheus-client>=0.19.0

# Abutre Bot dependencies (Phase 3.1)
SQLAlchemy>=2.0.23
# websockets - controlled by python-deriv-api (requires ==10.3)
python-socketio>=5.10.0
python-engineio>=4.8.0
alembic>=1.13.0
