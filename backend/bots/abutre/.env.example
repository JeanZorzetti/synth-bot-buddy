# ==================== ABUTRE BOT - ENVIRONMENT VARIABLES ====================

# ==================== STRATEGY PARAMETERS ====================
# Validated in backtest: 180 days, +40.25% ROI, 100% win rate

# Wait for N consecutive candles before entering (CRITICAL PARAMETER)
# Historical max: 18 velas | Your capacity: 10 levels | Optimal: 8
DELAY_THRESHOLD=8

# Maximum Martingale level (safety limit)
# Level 10 = $512 stake, $1,023 accumulated loss
MAX_LEVEL=10

# Initial stake per trade (USD)
INITIAL_STAKE=1.0

# Martingale multiplier (2.0 = classic doubling)
MULTIPLIER=2.0

# Starting bankroll (USD)
BANKROLL=2000.0

# Max drawdown tolerance (0.25 = 25% emergency shutdown)
MAX_DRAWDOWN_PCT=0.25

# ==================== DERIV API ====================

# Get your API token from: https://app.deriv.com/account/api-token
# IMPORTANT: Use DEMO token for testing!
DERIV_API_TOKEN=your_demo_api_token_here

# App ID (REQUIRED - register at https://api.deriv.com to get your own)
# Default: 1089 (public app_id for testing)
# For production: Create your own app at https://api.deriv.com
DERIV_APP_ID=1089

# WebSocket URL (do NOT change unless using different server)
DERIV_WS_URL=wss://ws.derivws.com/websockets/v3

# Asset to trade
# Options: 1HZ100V (V100), 1HZ75V (V75), 1HZ50V (V50)
SYMBOL=1HZ100V

# Contract type: CALL (buy) or PUT (sell)
CONTRACT_TYPE=CALL

# Duration (1 tick recommended for M1 strategy)
DURATION=1
DURATION_UNIT=t

# ==================== RISK MANAGEMENT ====================

# Spread/commission per trade (0.05 = 5%)
SPREAD_PCT=0.05

# Emergency stop if balance drops below this (USD)
MIN_BALANCE=500.0

# Enable auto-trading (true/false)
# false = Paper trading (monitor only, no execution)
AUTO_TRADING=false

# ==================== SYSTEM ====================

# Database path
DB_PATH=backend/bots/abutre/data/abutre.db

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log file path
LOG_FILE=backend/bots/abutre/logs/abutre.log

# WebSocket reconnect settings
WS_RECONNECT_DELAY=5
WS_MAX_RECONNECT_ATTEMPTS=10

# Rate limiting (requests per second)
MAX_REQUESTS_PER_SECOND=5

# ==================== NOTIFICATIONS (OPTIONAL) ====================

# Telegram Bot (optional)
# Get token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Email Alerts (optional)
EMAIL_ENABLED=false
EMAIL_FROM=
EMAIL_TO=
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# ==================== NOTES ====================
#
# BEFORE LIVE TRADING:
# 1. Test with DEMO account for 30 days (AUTO_TRADING=false)
# 2. Validate ROI > 5%, Win Rate > 90%
# 3. Monitor max drawdown < 30%
# 4. Start with SMALL bankroll ($200) for micro testing
# 5. Scale to $2000 ONLY after validation
#
# SAFETY:
# - NEVER commit this file with real API tokens
# - Add .env to .gitignore
# - Use different tokens for demo vs live
# - Monitor the bot 24/7 initially
